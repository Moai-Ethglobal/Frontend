# Huddle01（底线：Huddle 必须能用；否则 Meetings 功能不可用）
# 必填：HUDDLE_API_KEY、NEXT_PUBLIC_HUDDLE_PROJECT_ID
HUDDLE_API_KEY=
NEXT_PUBLIC_HUDDLE_PROJECT_ID=

# ENS（底线：必须集成；这里是可选配置。不填也能用，默认走公共网关）
ENS_RPC_URL=https://ethereum-rpc.publicnode.com
MAINNET_RPC_URL=https://ethereum-rpc.publicnode.com

# 链上集成（0208 优先级 #1：Integration）
# 如果要演示“真实合约交互”，至少需要填：NEXT_PUBLIC_MOAI_CONTRACT_ADDRESS + NEXT_PUBLIC_MOAI_CHAIN_ID
# 如果要演示 Factory 创建（可选）：NEXT_PUBLIC_MOAI_FACTORY_ADDRESS
# NEXT_PUBLIC_MOAI_RPC_URL：给 AA/读取用的 RPC（也可不填，使用卡片里手动配置）
NEXT_PUBLIC_MOAI_CONTRACT_ADDRESS=
NEXT_PUBLIC_MOAI_CHAIN_ID=
NEXT_PUBLIC_MOAI_FACTORY_ADDRESS=
NEXT_PUBLIC_MOAI_RPC_URL=

# AA（可选，但属于加分项：passkey/email 免 gas 体验）
# 方式一：填完整 Pimlico URL（NEXT_PUBLIC_PIMLICO_RPC_URL）
# 方式二：填 API KEY + CHAIN（NEXT_PUBLIC_PIMLICO_API_KEY + NEXT_PUBLIC_PIMLICO_CHAIN）
# 必填：NEXT_PUBLIC_AA_CHAIN_ID、NEXT_PUBLIC_AA_RPC_URL、Pimlico 其中一种方式
NEXT_PUBLIC_AA_CHAIN_ID=
NEXT_PUBLIC_AA_RPC_URL=
NEXT_PUBLIC_PIMLICO_API_KEY=
NEXT_PUBLIC_PIMLICO_CHAIN=
NEXT_PUBLIC_PIMLICO_RPC_URL=

# Arc Testnet（对话中 Hema 给的链信息）
# ARC_TESTNET_CHAIN_ID=5042002

# Meeting gating（可选，但属于“真门禁”亮点：只有链上成员才能拿到会议 token）
# 如果要开启门禁：必须把这三个都填上（服务端用，不暴露给浏览器）
MOAI_RPC_URL=
MOAI_CONTRACT_ADDRESS=
MOAI_CHAIN_ID=

